# ── Ядро ──────────────────────────────────────────────────────────────────────
torch>=2.2.0                  # GPU-вычисления; устанавливай с CUDA: https://pytorch.org/get-started/
accelerator>=0.27.0
accelerate>=0.27.0            # для device_map и квантизации

# ── Модели и эмбеддинги ────────────────────────────────────────────────────────
sentence-transformers>=3.0.0  # BGE-M3 и BGE Reranker
transformers>=4.43.0          # Llama 3.1, AutoModelForCausalLM
bitsandbytes>=0.43.0          # NF4/INT8 квантизация (требуется NVIDIA GPU)
huggingface-hub>=0.23.0       # авторизация HF_TOKEN, whoami

# ── Векторная БД ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Граф агента ───────────────────────────────────────────────────────────────
langgraph>=0.1.0

# ── Утилиты ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # загрузка .env файла
tqdm>=4.66.0                  # прогресс-бары
requests>=2.31.0              # HTTP-запросы
